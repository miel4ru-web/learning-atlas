# Learning Atlas — CSV 카드 가져오기 형식

> 이 문서는 "카드를 CSV로 대량 생성해 달라"고 다른 LLM에게 시킬 때 프롬프트에 그대로
> 붙여주기 위한 명세다. 실제 파싱 로직은 `src/core/csvImport.ts`에 있고, 이 문서는 그
> 코드의 동작을 그대로 옮겨 적은 것이다 — **CSV 임포터가 새 문제 유형을 지원하게 되거나
> 파싱 규칙이 바뀌면 이 파일도 같은 커밋에서 갱신한다.** 반대로 이 문서에만 있고 코드에는
> 없는 내용을 추가하지 않는다 — 둘이 어긋나면 다른 LLM이 만든 CSV가 전부 가져오기
> 실패로 돌아간다.

## 지금 CSV로 만들 수 있는 문제 유형 (3종)

Learning Atlas의 카드 유형은 6가지(플래시카드 / 빈칸 채우기 / 4지선다 / 코드 / 단답형 /
자기 설명)이지만, **CSV 가져오기는 이 중 3가지만 지원한다: 플래시카드, 빈칸 채우기,
단답형.** 4지선다·코드·자기 설명 카드는 CSV로 만들 수 없다 — "카드" 탭에서 직접
만들어야 한다. (이 셋 중 하나라도 CSV로 지원하게 되면 이 문단과 "유형별 필수 값"에
새 절을 추가할 것.)

## 파일 규칙 (기본 규칙 — 항상 유지)

- 인코딩: UTF-8. 맨 앞 BOM(엑셀이 붙이는 것)은 자동으로 제거되므로 있어도/없어도 된다.
- 줄바꿈: LF/CRLF 모두 허용, 마지막 줄에 줄바꿈이 없어도 된다.
- 구분자는 쉼표(`,`) 고정.
- 값에 쉼표·줄바꿈·큰따옴표가 들어가면 그 값 전체를 큰따옴표로 감싼다(RFC 4180). 값
  안의 큰따옴표는 두 번 반복(`""`)해서 이스케이프한다. 예: `"그는 ""안녕""이라 했다"`
- 첫 줄은 반드시 헤더(열 이름)다.
- 헤더에서 알아볼 수 있는 열 이름이 하나도 없으면 파일 전체를 거부한다.
- 모든 셀이 빈 줄은 조용히 건너뛴다(오류로 세지 않는다).
- 한 줄이 잘못돼도 그 줄만 건너뛰고 나머지는 정상 처리한다 — 파일 전체가 실패하지 않는다.

## 열(컬럼) 이름 — 한국어/영어 둘 다 인식 (대소문자·앞뒤 공백 무시)

| 의미 | 인식하는 헤더 이름 |
| --- | --- |
| 문제 유형 | `type`, `타입`, `유형` |
| 플래시카드 앞면 | `front`, `앞면`, `질문`, `q` |
| 플래시카드 뒷면 | `back`, `뒷면`, `답`, `a` |
| 빈칸 카드 본문 | `text`, `본문`, `빈칸` |
| 단답형 질문 | `prompt`, `문제` |
| 단답형 정답 | `answers`, `정답` |
| 분류(지식 요소) | `kc`, `분류`, `지식요소`, `지식 요소` |

필요 없는 열은 비워두거나 아예 빼도 된다. 여러 문제 유형을 한 파일에 섞으려면 `type`
열을 두고, 유형별로 안 쓰는 칸은 비워 둔다(아래 "여러 유형을 한 파일에 섞는 예시" 참고).

## `type` 열 값

| 유형 | 인식하는 값 |
| --- | --- |
| 플래시카드 | `flashcard`, `플래시카드`, `카드` |
| 빈칸 채우기 | `cloze`, `빈칸`, `빈칸 채우기` |
| 단답형 | `short`, `단답형`, `단답` |

`type` 열을 비우거나 아예 빼면, 아래 순서로 내용을 보고 하나만 골라 추론한다(먼저
맞는 조건 하나만 적용):

1. `text`에 `{{`가 있으면 → 빈칸 채우기
2. `prompt`와 `answers`가 둘 다 값이 있으면 → 단답형
3. `front`와 `back`이 둘 다 값이 있으면 → 플래시카드
4. 셋 다 아니면 → 그 줄은 오류("어떤 카드인지 알 수 없습니다")

`type` 열에 위 표에 없는 값이 들어 있으면 그 줄만 오류("모르는 타입입니다: ...")로
건너뛴다.

## 유형별 필수 값

### 플래시카드 (`flashcard`)

필수: `front`, `back` 둘 다 값이 있어야 한다. 하나라도 비면 그 줄은 오류.

```csv
type,front,back,kc
flashcard,인출 연습이란?,기억에서 꺼내 보는 것,학습 원리 기초
```

### 빈칸 채우기 (`cloze`)

필수: `text`에 `{{...}}` 형태의 빈칸이 최소 하나 있어야 한다(`{{`와 `}}`가 둘 다 있어야
함). 빈칸이 없으면 그 줄은 오류.

```csv
type,text,kc
cloze,물의 화학식은 {{H2O}}이다,화학 기초
```

### 단답형 (`short`)

필수: `prompt`(질문)와 `answers`(정답) 둘 다 값이 있어야 한다.

`answers`에 정답을 여러 개(동의어·다른 표기) 넣을 때는 **파이프(`|`)**로 구분한다.
쉼표(`,`)는 CSV 구분자와 겹치므로 정답 구분에 쓰지 않는다.

```csv
type,prompt,answers,kc
short,물의 화학식은?,물|H2O,화학 기초
```

## 분류(`kc`) 열

- **이름**으로 적는다(id 아님). 대소문자·앞뒤 공백은 무시하고 같은 이름으로 맞춘다.
- 이미 앱에 있는 이름이면 그 분류에 카드가 붙는다.
- 없는 이름이면 새 분류가 자동으로 만들어진다. 같은 파일 안에서 같은 이름이 여러 번
  나오면 전부 하나의(새로 만들어진) 분류로 묶인다.
- 비워두면 분류 없이 들어간다.

## 여러 유형을 한 파일에 섞는 예시

```csv
type,front,back,text,prompt,answers,kc
flashcard,인출 연습이란?,기억에서 꺼내 보는 것,,,,학습 원리 기초
cloze,,,물의 화학식은 {{H2O}}이다,,,화학 기초
short,,,,물의 화학식은?,물|H2O,화학 기초
```

## 이 문서를 다른 LLM에게 줄 때

카드를 대량 생성해 달라고 다른 LLM에게 요청할 때는 이 문서 전체를 프롬프트에 붙이고,
"위 형식을 정확히 지켜서 CSV를 만들어 달라"고 지시한다. 특히 다음을 강조할 것:

- 지원하는 유형은 3가지뿐(플래시카드/빈칸 채우기/단답형)이다 — 그 외 유형(4지선다/코드/
  자기 설명)은 요청하지 말 것.
- 빈칸 카드는 `{{ }}`를 반드시 포함할 것.
- 단답형의 정답이 여러 개면 쉼표가 아니라 `|`로 구분할 것.
- 값에 쉼표·줄바꿈이 들어가면 큰따옴표로 감쌀 것.

## 유지 보수 규칙

이 문서는 `src/core/csvImport.ts`의 실제 동작을 설명한 것이다. 다음 중 하나라도
코드에서 바뀌면 **같은 커밋에서 이 문서도 반드시 갱신한다**:

- CSV 가져오기가 지원하는 문제 유형이 늘거나 줄 때(지금 3종 → 예: 4지선다 추가)
- 열 이름 별칭(`COLUMN_ALIASES`)이 추가/변경될 때
- `type` 값 별칭(`TYPE_ALIASES`)이 추가/변경될 때
- 유형 추론 규칙의 순서·조건이 바뀔 때
- 정답 구분자(현재 `|`)가 바뀔 때
- 필수/선택 값 규칙이 바뀔 때

새 문제 유형을 CSV 임포터에 추가할 때는 다음 순서로 이 문서를 갱신한다:

1. "지금 CSV로 만들 수 있는 문제 유형" 목록에 추가
2. "`type` 열 값" 표에 새 별칭 추가
3. "유형별 필수 값"에 새 소제목·필수 값 설명·예시 CSV 추가
4. 필요하면 "여러 유형을 한 파일에 섞는 예시"에도 반영
